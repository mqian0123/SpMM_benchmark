g <- graph.full(5) %du% graph.full(5) %du% graph.full(5)
g <- add.edges(g, 
g <- graph.full(4) 
library(igraph)
g <- graph.full(4) 
g
g <- graph.empty(4) 
g[from=c(1,2,3), to=c(2,3,4)] <- c(5,1,5) 
g
g[from=c(1,2,3), to=c(2,3,4),attr="weight"] <- c(5,1,5) 
g
comm <- multilevel.community (g)
uig = as.undirected(g, "collapse”)
comm <- multilevel.community (uig)
uig = as.undirected(g, "collapse")
comm <- multilevel.community (uig)
comm
uig
exit
quit()
